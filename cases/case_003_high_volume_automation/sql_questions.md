# SQL Questions

1. Count events by account for `case_003`.
2. Find the first and last target event.
3. Calculate approximate events per minute for `acct_4102`.
4. Count events by `prompt_category`.
5. Count events by `policy_signal`.
6. Find all events with `risk_score >= 80`.
7. Use a CTE to compare target volume to average account volume.
8. Bucket timestamps by hour and count events.
9. Find accounts sharing the target's IP.
10. Recommend whether a rate limit is supported by the data.

