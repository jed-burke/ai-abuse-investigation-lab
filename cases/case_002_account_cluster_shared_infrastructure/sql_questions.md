# SQL Questions

1. Count events by account for `case_002`.
2. Join `ai_usage_logs` to `account_profiles` and show account age, trust tier, and event count.
3. Find IPs used by `acct_3104`.
4. Find other accounts that used those same IPs.
5. Count distinct user agents by account.
6. Count policy signals by account.
7. Use a CTE to find accounts with both shared IP and average risk above 50.
8. Use a self-join to produce account pairs that share an IP in `case_002`.
9. Find the first and last event per account.
10. Write one query that supports or challenges cluster coordination.

