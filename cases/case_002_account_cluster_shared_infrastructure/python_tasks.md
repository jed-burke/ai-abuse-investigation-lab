# Python Tasks

1. Load `data/synthetic/case_002_logs.csv`.
2. Build a dictionary of `ip_address -> set(account_id)`.
3. Return IPs used by more than one account.
4. Build `account_id -> set(policy_signal)`.
5. Write `shared_entities(rows, field)` for IPs or user agents.
6. Score each account with event count, average risk, max risk, and shared IP count.
7. Print a cluster summary for accounts linked to `acct_3104`.
8. Explain one limitation of shared-IP linkage.

