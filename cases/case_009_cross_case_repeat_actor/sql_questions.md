# SQL Questions

1. Find all cases involving `acct_9009`.
2. Find IPs used by `acct_9009`.
3. Find accounts sharing those IPs across all cases.
4. Count shared policy signals between `acct_9009` and each related account.
5. Join account metadata for related candidates.
6. Use a self-join to build account-pair shared IP counts.
7. Use a CTE to rank related accounts by shared IP and shared policy signals.
8. Use `ROW_NUMBER` to find each account's highest-risk event.
9. Find accounts with prior enforcement and high average risk.
10. Write a query supporting repeat-actor suspension.

