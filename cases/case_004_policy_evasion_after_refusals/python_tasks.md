# Python Tasks

1. Load `case_004_logs.csv`.
2. Build a sorted timeline for `acct_5901`.
3. Identify events that immediately follow a refusal.
4. Calculate risk deltas between adjacent events.
5. Count semantic retry signals.
6. Write `events_after_refusal(rows, account_id)`.
7. Produce a short narrative timeline.

